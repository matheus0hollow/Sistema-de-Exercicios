##1. Analise o Código, explicando o que ocorre (pode ser linha a linha):
#lista = [5, 10]  #cria uma lista que contém os elementos 5 e 10
#lista.append(15) #adiciona o elemento 15 ao final da lista
#print(len(lista)) #analisa quantos elementos tem na lista e imprime a quantidade(nesse caso 3)

##2. Analise o Código, explicando o que ocorre (pode ser linha a linha):
#lista = [1, 2, 3] #cria uma lista com os elementos 1, 2 e 3
#lista.insert(1, 10) #insere o elemento 10 posição indice 1
#print(lista) #Imprime os elementos na lista(nesse caso [1, 10, 2, 3]

##3. Analise o Código, explicando o que ocorre (pode ser linha a linha):
#lista = [2, 4, 6] #cria uma lista com os elementos 2, 4 e 6
#print(lista[1] * 2) #imprime o resultado do elemento de posição 1 vezes 2(nesse caso 8)

##4. Analise o Código, explicando o que ocorre (pode ser linha a linha):
#dados = {'x': 1, 'y': 2} #cria um dicionario com as chaves x e y e os valores 1 e 2 respectivamente
#print(dados['x'] + dados['y']) #imprime a soma dos valores de x e y(nesse caso 3)

##5. Analise o Código, explicando o que ocorre (pode ser linha a linha):
#dados = {'a': 10} #cria um dicionario com a chave a e o valor 10
#print(dados.get('b', 5)) #procura no dicionario 'dados' a chave 'b' para imprimir seu valor
                        #como não existe a chave, imprime o valor default(5)

##6. Analise o Código, explicando o que ocorre (pode ser linha a linha):
#nums = [1, 2, 3] #cria uma lista com os elementos 1, 2 e 3
#total = 0 #a variavel total recebe 0
#for n in nums: #inicia um loop, para cada elemento na lista nums faça:
    #total += n #total recebe o proprio valor mais o valor n atual. Isso se repete para cada elemento da lista
#print(total) #imprime o valor total, nesse caso 6

##7.